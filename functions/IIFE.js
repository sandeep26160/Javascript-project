//Immediately Invoked Function Expressions(IIFE)
(function chai(){
    console.log(`DB CONNECTED`);
})();
// ; and lago  at the end for the stop of exection   if not use show error in code

(function chaiAurCode(){
    console.log(`DB IS NOT CONNECTED`)
})()
